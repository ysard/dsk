--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -51,17 +51,17 @@
 SET (GATB_CORE_EXCLUDE_EXAMPLES  1)
 
 # GATB CORE
-include (GatbCore)
+find_package(gatbcore 1.3.0)
 
 ################################################################################
 # TOOLS
 ################################################################################
 
 # We add the compilation options for the library
-add_definitions (${gatb-core-flags})
+add_definitions (${GATBCORE_DEFINITIONS})
 
 # We add the gatb-core include directory
-include_directories (${gatb-core-includes})
+include_directories (${GATBCORE_INCLUDE_DIRS})
 
 # We add the path for extra libraries
 link_directories (${gatb-core-extra-libraries-path})
@@ -72,7 +72,7 @@
 include_directories (${PROGRAM_SOURCE_DIR})
 file (GLOB_RECURSE  ProjectFiles  ${PROGRAM_SOURCE_DIR}/*)
 add_executable(${PROJECT_NAME} ${ProjectFiles})
-target_link_libraries(${PROJECT_NAME} ${gatb-core-libraries})
+target_link_libraries(${PROJECT_NAME} ${GATBCORE_LIBRARIES})
 
 
 ################################################################################
--- a/utils/CMakeLists.txt
+++ b/utils/CMakeLists.txt
@@ -3,16 +3,16 @@
 ################################################################################
 
 # We add the compilation options for the library
-add_definitions (${gatb-core-flags} " -Wno-ambiguous-member-template")
+add_definitions (${GATBCORE_DEFINITIONS} " -Wno-ambiguous-member-template")
 
-include_directories (${gatb-core-includes})
+include_directories (${GATBCORE_INCLUDE_DIRS})
 
 #list (APPEND PROGRAMS dsk2ascii filter_by_previous_contigs)
 list (APPEND PROGRAMS dsk2ascii)
 
 FOREACH (program ${PROGRAMS})
   add_executable(${program} ${program}.cpp)
-  target_link_libraries(${program} ${gatb-core-libraries})
+  target_link_libraries(${program} ${GATBCORE_LIBRARIES})
 ENDFOREACH (program)
 
 ################################################################################
